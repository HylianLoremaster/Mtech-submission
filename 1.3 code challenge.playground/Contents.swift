import Cocoa

let name = "Brayden"
let age = 23

print("Hi, my name is \(name), and I'm \(age) years old!")

func countLetters(of word: String) -> Int
{
    return word.count
}

func add(_ first: Int, and second: Int) -> Int
{
    return first + second
}
